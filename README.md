
  # Admin Dashboard for DAN-SPOT

  This is a code bundle for Admin Dashboard for DAN-SPOT. The original project is available at https://www.figma.com/design/PCtyZLatF3GaOoIoWE4mWV/Admin-Dashboard-for-DAN-SPOT.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  